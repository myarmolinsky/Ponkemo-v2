import { combineReducers } from "redux";
import alert from "./alert";

export default combineReducers({ alert }); //'combineReducers()' takes in a object which will have any and all reducers we create
